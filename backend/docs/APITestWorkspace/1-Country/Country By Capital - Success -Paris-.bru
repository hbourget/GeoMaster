meta {
  name: Country By Capital - Success -Paris-
  type: http
  seq: 3
}

get {
  url: {{country_service_url}}/countries/capital/Paris
  body: none
  auth: none
}

tests {
  test("should be able to get country byCapital", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
