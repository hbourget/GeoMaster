meta {
  name: Country By Flag - Failure
  type: http
  seq: 6
}

get {
  url: {{country_service_url}}/countries/flag/InvalidFlagName
  body: none
  auth: none
}

tests {
  test("should not be able to get country byFlag", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(404);
  });
}
